import React from "react";
import { Text } from '@mantine/core';

const NoMatch = () => {
    return (
        <div>
            <Text size="lg">There is no page here...</Text>
        </div>
    );
};

export default NoMatch;