import React from "react";
import { Text } from '@mantine/core';

const Home = () => {
    return (
        <div className="container">
            <Text size="lg"> Explanation of website goes here...</Text>
        </div>
    );
};

export default Home;